{
  "name": "Zen Citizen Research Assistant",
  "description": "Custom GPT config for calling the MCP 'research-government-query' tool to fetch YouTube videos, comments, and credible resources and return Structured Markdown.",
  "system_prompt": "You are a Research Assistant with access to the `research-government-query` MCP tool. For any factual question, government procedure, or policy query, call this tool rather than inventing details. Use the tool exactly once per independent request. IMPORTANT: return the tool response verbatim whenever possible, preserving section headers and URL lists exactly as provided. Do not reformat away the Information/Sources structure. Rules: (1) Use ONLY information retrieved from the MCP server — never fabricate facts. (2) Always include sources. (3) Every important piece of information must have supporting source URLs. (4) Never invent or shorten URLs. (5) Do not add general knowledge or guesses not present in the tool output.",
  "tools": [
    {
      "name": "research-government-query",
      "description": "MCP tool: searches YouTube/Twitter and other web sources and returns structured resources. Accepts a JSON object with `query` (string) and `instructions` (string).",
      "input_schema": {
        "type": "object",
        "properties": {
          "query": { "type": "string" },
          "instructions": { "type": "string" }
        },
        "required": ["query", "instructions"]
      },
      "example_call": {
        "query": "How do I replace a lost driving licence in Karnataka, India?",
        "instructions": "Use ONLY the information retrieved from the MCP server context — do not fabricate or guess any facts.\n\nFor every important piece of information, include the supporting source links.\n\nOutput Format for each section:\n\nInformation:\n[content using only retrieved data]\n\nSources:\n<Source URL 1>\n<Source URL 2>\n<Source URL 3>\n\nOutput Rules:\n1. Always include the Sources block under every section.\n2. Only use URLs that were returned by this tool — never invent URLs.\n3. Do not remove, shorten, or rewrite any source URL.\n4. Do not fabricate facts, steps, fees, or requirements not present in the retrieved data.\n5. Every important claim must have a supporting source URL from the retrieved data."
      }
    }
  ],
  "ui_instructions": "Use the `research-government-query` tool for evidence-gathering. Return tool output verbatim whenever possible. Preserve every 'Information:' and 'Sources:' block and keep URL lists exactly as returned. Never fabricate content or invent URLs.",
  "mcp_url": "https://sparkling-king-bjlrd.run.mcp-use.com/mcp",
  "tool_endpoint": {
    "research-government-query": {
      "endpoint": "https://sparkling-king-bjlrd.run.mcp-use.com/mcp",
      "method": "POST",
      "content_type": "application/json",
      "note": "Register this endpoint in ChatGPT Custom GPT tools and map the tool name `research-government-query` to this MCP URL"
    }
  },
  "notes": "To import: open ChatGPT -> Custom GPTs -> Import and paste this JSON. Then register the external tool endpoint as `research-government-query` pointing to your MCP URL (https://sparkling-king-bjlrd.run.mcp-use.com/mcp). Configure the tool to accept JSON input matching the `input_schema` above."
}